// Generated by CoffeeScript 1.7.1
(function() {
  (function($) {
    var FADE_SPEED, fillDateTimestamps, init;
    FADE_SPEED = 75;
    init = function() {
      return fillDateTimestamps();
    };
    fillDateTimestamps = function() {
      return $('*[data-with-timezone]').each(function() {
        var el, m, ms;
        el = $(this);
        ms = parseInt(el.data('date-timestamp'), 10) * 1000;
        m = window.moment(ms);
        el.html('<span class="time">' + m.format("M.DD.YYYY h:mm A") + ' <span class="tz">' + m.format('Z') + '</span>' + '</span>');
      });
    };
    return init();
  })(jQuery);

}).call(this);
